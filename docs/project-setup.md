# Project setup cleanup

## Requirement

Before the initial commit, the generic Astro starter boilerplate should be replaced with project-specific placeholders so the repository starts from a clean baseline.

## What changed

- Replaced the Astro starter README with a concise project README for HTML PPT.
- Replaced the default `src/pages/index.astro` content with a minimal HTML PPT placeholder page.
- Removed default favicon assets because they were Astro starter branding and are not yet project-specific.
- Removed generated VS Code starter settings to avoid committing editor-specific boilerplate.

## Open issues

The project does not yet have final branding, favicon assets, or real presentation content. These can be added when the product direction and visual identity are defined.
